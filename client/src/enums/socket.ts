export enum Action{
    JOINED,
    LEFT,
    RENAME,
}
export enum Event{
    CONNECT = 'connect',
    DISCONNECT = 'disconnect',
}
export enum MessageType{
    CHAT = 'chat',
    NOTIFICATION = 'notification',
}